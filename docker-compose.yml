version: "3.8"
services:
  cities-client:
    build:
      context: .
      dockerfile: Dockerfile
    command: npm run start
    container_name: frontend
    ports:
      - "3000:3000"
    volumes:
      - ./:/frontend
      - /frontend/node_modules